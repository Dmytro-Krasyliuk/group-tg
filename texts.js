// write (create, append) / read (get)

// 102
// let function new super const true false - зарезервовані слова (~40 штук)
// variables, names function

// let for1 = 34

// server = 100 bot 40 000  2000
// bot.setMyCommands()

// pc (server) - on 40 000 1 year
// 0100100001001 processor 10-50 000 000 000

// 🎲", “🎯", “🏀", “⚽", “🎳", or “🎰


// Фото товару
// Ціна
// Опис
// Кнопки:
// Купити
// В обране
// Задати питання




// b -bold 
// s - strike
// u - underline
// i - italic
// https://t.me/money_farmer_game_bot


// ~~ - s 
// __ - i 
//  \*  * - b 
// readme.md

// HTML Markdown (v2) simple

  //  bot.sendMessage(
  //    id,
  //    "  <code>code</code>     <a href='https://google.com'>link</a> <b>hello</b><i>hello</i><s>asdf</s><u>asdf</u><span class='tg-spoiler'>hello</span>",
  //    { ...keyboard, parse_mode: "HTML" }
  //  ); 
  //  let diceMessage = await bot.sendDice(id, { emoji: "⚽" }); 

  //  bot.sendMessage(id, 'keyboard inline', keyboard2)

  //  if (diceMessage.dice.value > 3) {
  //     bot.sendMessage(id, 'Super')
  //  } else {
  //     bot.sendMessage(id, "Wrong");

  //  }


  //  console.log(diceMessage.dice.value)

  //  bot.sendPhoto(
  //    id,
  //    "https://www.5.ua/media/pictures/original/251454.jpg?t=1656670805", 
  //  ); 



    // bot.answerCallbackQuery({ callback_query_id: message.message.id });
  // {
  //   polling: true;
  // }   зациклений безкінечно працював





let a = 255000;
a.toString().slice(0, 1);


var photo1 = "https://content.rozetka.com.ua/goods/images/big/330075700.jpg";
var price1 = 22000;
var discount1 = 70;
var description1 =
  "Классическая футболка из органического хлопка с брендовым логотипом на груди.";
// business + youtube + english + it + finance (buisness) + design  



    //   work with API API - працювати з готовим продуктом
    //  інтеграція = ідея +  телеграм + liqpay(pb24) + nova post
    // 10 000 -> 2 9998
    // JS speed C#


    async function saveClient() {
      fs.writeFile(
        "database.txt",
        JSON.stringify(clientTemplate),
        (error) => {}
      );
    }

    saveClient();